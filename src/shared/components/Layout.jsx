'use strict';

let $ = require('jquery');
let React = require('react');

let Layout = React.createClass({

  render () {
    return (
      <div>Hello garbage</div>
    );
  }
});

module.exports = Layout;
